[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mbi_paper"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "matplotlib == 3.7.3",
    "numpy",
    "pandas",
    "pyarrow",
    "seaborn",
    "tqdm"
]
